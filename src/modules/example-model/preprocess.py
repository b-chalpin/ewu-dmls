def preprocess():
    '''
        Main preprocessing method for the dataset. This is the 
        entrypoint for all dataset preprocessing.

        WARNING: This method must not be renamed or return anything different!

        Returns:
            x_train: 
            y_train:
            x_test:
            y_test: 
    '''
    x_train, y_train, x_test, y_test = None # define your dataset here

    # your preprocessing code here

    return x_train, y_train, x_test, y_test
