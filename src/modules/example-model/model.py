def create_model(x_train, y_train, x_test, y_test, model_config):
    ''' 
        Main train method

        Params:
            x_train:
            y_train:
            x_test:
            y_test:
            model_config:

        Returns:
            model: 
    '''

    model = None # your TensorFlow model here

    return model

